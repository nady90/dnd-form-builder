import { IQuestionTypeButton } from "./QuestionTypeButton";

const section: IQuestionTypeButton = {
  variant: "Sections",
};

const table: IQuestionTypeButton = {
  variant: "Tables",
};

export const mockQuestionTypeButtonProps = {
  section,
  table,
};
